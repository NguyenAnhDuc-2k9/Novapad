Prompt (ConPTY)
- Open Tools > Prompt (or Ctrl+Shift+P) and confirm window opens with Input focus.
- Run `cmd.exe`: type `echo hello` and confirm output appears and is selectable.
- Switch program to Codex CLI in Options, reopen Prompt, run an interactive command, confirm output streams and Ctrl+C interrupts.
